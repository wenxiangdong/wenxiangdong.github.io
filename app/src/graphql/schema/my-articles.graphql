query { 
  repository (name: "my-articles", owner: "wenxiangdong") { 
    object(expression: "master:") {
      ... on Tree {
            entries {
                oid
                name
                type
            }
        }
    }
  }
}